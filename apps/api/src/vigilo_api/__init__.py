"""Vigilo API — the HTTP surface (docs/modules.md §12).

Phase 0 scope: a health/version bootstrap only, to prove the dev loop (uv
workspace, Docker Compose services, a running server) works end to end.
Business endpoints (scan submission, reports, projects, billing) start in
Phase 3 per docs/build-roadmap.md.
"""
